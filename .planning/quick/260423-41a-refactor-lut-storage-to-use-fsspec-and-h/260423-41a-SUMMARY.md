# Quick Task 260423-41a Summary

## Changes Made
- **lut.py**:
  - Removed `download_lut`, `get_default_lut_release_url`, and the imports used for downloading (`shutil`, `urllib.request`, `zipfile`, `importlib.metadata`).
  - Added `get_default_bucket_uri` which returns `hf://bucket/frandorr/aer-data`.
  - Refactored `load_utm_zone_lut` to take `bucket_uri` instead of `lut_dir`. It now maps the remote path dynamically via `fsspec.get_mapper` and hands it to `zarr.open`.
- **core.py**:
  - Replaced `lut_dir` with `bucket_uri` handling.
  - Used the default bucket URI if not provided in `extract_params`.
- **Tests**:
  - Ensured `load_utm_zone_lut` still seamlessly handles local `Path` objects (e.g. `tmp_path`) from tests by deducing the protocol from the string prefix.
  - All `test/components/aer/extract_aws_goes/` pass successfully (24 passed).

## Impact
The extraction engine now accesses pre-computed LUTs directly from the remote Hugging Face Bucket via streaming / dynamic chunk mapping instead of requiring a complete download and local decompression step beforehand. This eliminates network overhead and disk space requirements for storing the entire `zarr` archives.
